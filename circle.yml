machine:
  php:
    version: 5.5.11

test:
  override:
    - ./bin/test.sh
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/phpunit
    - find . -type f -regex ".*/var/report/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/phpunit/ \;
